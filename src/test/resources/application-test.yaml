spring:
  application:
    name: reops-data-filter-test
  kafka:
    topic: test-topic
    bootstrap-servers: ${spring.embedded.kafka.brokers}

logging:
  level:
    root: ERROR
    no.nav.reops: INFO